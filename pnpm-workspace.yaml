onlyBuiltDependencies:
  - sharp
  - unrs-resolver

# Supply-chain hardening: refuse to install package versions younger than
# 14 days (20160 minutes). Mirrors the Dependabot cooldown in
# .github/dependabot.yml.
minimumReleaseAge: 20160
minimumReleaseAgeStrict: true
